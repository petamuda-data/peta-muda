// Intake queue endpoint (Supabase project URL + publishable anon key).
// The anon key only exposes the passphrase-guarded intake_* RPCs — it is safe
// to ship publicly. Fill these in after provisioning (see HANDOFF.md).
window.OPS = {
  url: '',
  anonKey: '',
}
